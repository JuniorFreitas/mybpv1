import ControleUsuarios from "../../../components/relatorios/controleusuarios/ControleUsuarios";

const app = new Vue({
    el: '#app',
    data: {},
    components: {
        ControleUsuarios
    },
});
